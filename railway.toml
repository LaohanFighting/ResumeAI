[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# 必须通过 shell 展开 PORT，否则 uvicorn 会收到字面量 $PORT
startCommand = "sh -c 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
