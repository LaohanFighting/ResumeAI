[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# 使用启动脚本，确保 PORT 正确传入（Railway 会覆盖 Dockerfile CMD 时也一致）
startCommand = "/app/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
